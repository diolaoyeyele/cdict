#include <stdio.h>
#include <stdlib.h>

int main()
{

    /* printf("%d",35+63);*/
    printf("%f\n", pow(2, 3) );/*raised to the power,only works with float */
    printf("%f\n", sqrt(25) );/*prints square root */
    printf("%f\n", ceil(45.23333) );/*rounds up regardless of first decimal */
    printf("%f\n", round(67.09564) );/*rounds up or down based on first decimal */
    printf("%f\n", floor(67.89564) );/*rounds fown regardless of first decimal */
    printf("%f\n", pow(2, 3) );
    return 0;
}